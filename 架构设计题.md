# 架构设计题



## **模块一：Context / Performance**

### 一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？


**本项目如何压缩？**

如果一个 Session 连续聊 200 轮，本项目不会等到第 200 轮才一次性压缩，而是采用“**滚动摘要**”：每次调用模型前检查预算，一旦超过软上限，就把较早对话合并进累计摘要，只保留最近的完整对话。**触发摘要条件**是以下任一情况：

```
- 完整 Prompt 估算超过 6000 Token 软上限；
- 摘要和消息超过兼容阈值 24000 字符；
- 若模型仍报告上下文过长，返回上下文过长错误。
```

**滚动摘要**：将较早对话与已有摘要合并成新摘要，保留用户目标、偏好、关键事实、工具结论、约束和未完成事项。

**裁剪大型结果**：较早且过大的搜索、天气等工具结果先截断，必要时可重新调用工具获取。

**保留完整历史**：压缩只影响发送给模型的 Context，原始 200 轮对话仍保存在 SQLite 中。

**如何保证压缩后仍然流畅？**

**1、摘要负责长期连续性**：摘要作为 `<session_summary>` 注入 System Prompt，并明确告诉模型：它是历史事实记忆，不是新的用户指令。
摘要模型被要求保留：

```
- 当前用户目标；
- 已确认事实与用户偏好；
- 关键实体、名称和参数；
- 工具结论、来源和查询时间；
- 会话约束；
- 待办事项；
- 尚未解决的问题；
- 下一步继续对话所需信息。
```



**2、保留近期原文**：保留最近约4轮完整消息，使模型能够理解“这个方案”“继续刚才的话题”等局部指代。这里配置的是“对话轮数”而非“消息数”；一次工具调用可能产生多条消息。

**3、不拆开完整轮次和工具调用**：压缩切分时不拆开 `tool_use` 和 `tool_result`，避免工具调用链断裂。

## **模块二：Memory**

### 你理解的 Agent memory 经典框架是什么？它的发展趋势是什么，最头部的玩家在怎么做？

最有代表性的理论框架是 CoALA，它将记忆分成工作记忆以及语义、情景、程序性长期记忆。
**1、工作记忆（Working Memory）**

工作记忆就是当前推理真正放进 Context Window 的内容，例如：

```
- 当前用户问题；
- 最近几轮原始对话；
- 当前任务计划和 Todo；
- 最近的工具调用结果；
- 从长期记忆中召回的相关内容。
```

它容量有限、访问速度快，类似计算机内存。Context 压缩、滑动窗口、Token 预算管理都属于工作记忆管理。

**2、情景记忆（Episodic Memory）**

情景记忆保存“过去发生了什么”，通常带有时间、场景和来源；原始聊天、Session 摘要、工具执行轨迹和任务经历都可以属于情景记忆。

**3、语义记忆（Semantic Memory）**

语义记忆保存从历史事件中提炼出的稳定事实，而不是完整经历；它适合跨 Session 个性化，也应支持冲突、时效和版本管理。例如“用户住在北京”后来变成“用户搬到上海”，不能简单保留两个相互冲突的事实，而应标记新事实取代旧事实。

**4、程序性记忆（Procedural Memory）**

程序性记忆保存“应该怎么做”，它让 Agent 不只是记得事实，还能从过去的成功和失败中改善行为。包括：

```
用户要求的固定回答风格；
成功处理某类任务的步骤；
Agent 使用工具的策略；
从失败中总结出的操作规则；
可复用的 Workflow、Skill 或代码。
```

**5、原始事件日志（Source of Truth）**

成熟系统通常还会保留一份追加式原始记录，摘要和结构化事实可能出错，因此原始记录应作为事实来源。压缩通常只改变模型看到的 Context，不应直接销毁原文。包括：

```
原始用户消息；
Agent 回答；
工具参数和结果；
时间、Session 和消息 ID；
记忆提取来源。
```

**发展趋势**

1. 从“聊天记录”走向“分层认知状态”：早期系统主要保留最近 N 条消息；现在逐渐拆成不同类型的记忆采用不同的写入、更新和遗忘策略。

2. 从单 Session 走向跨 Session、跨应用

3. 从纯向量检索走向混合检索

4. 从静态事实走向时间感知记忆：未来 Memory 不只保存“事实是什么”，还需要保存：事实何时成立；何时失效；来源是什么；被哪条新事实取代；当前查询应该使用哪个时间版本。

6. 从不可见自动记忆走向可解释、可控制：Agent 记住了什么；为什么本次回答使用这条记忆；记忆来自哪次对话；如何纠正、删除和关闭；如何使用临时会话避免读写记忆。未来 Memory 的竞争不只看“记得多不多”，也看“是否可信、可控、可审计”。
7. 从同步写入走向后台巩固：每次用户发言都调用大模型提取记忆，会增加延迟和成本。更合理的趋势是：热路径只记录原始事件和少量关键状态；后台异步做摘要、去重、冲突处理和图谱更新；高价值显式指令立即写入。

**ChatGPT ：**

- Saved Memories：明确保存的偏好、目标和个人信息；
- Reference Chat History：从历史会话中选择相关内容进行个性化。

当前产品还强调自动管理、按新近度和使用频率调整优先级、记忆摘要、来源展示、版本恢复、手动优先或降级，以及 Temporary Chat。它代表的方向是：**长期个性化 + 自动巩固 + 用户可见和可控制**。

**Gemini**：

Gemini 使用过去聊天进行跨会话个性化，并尝试与 Google 生态中的数据源结合。用户可以关闭 Memory、删除相关历史、直接纠正错误记忆，并检查回答是否使用了历史信息。它的优势方向是：**Memory 与邮件、文档、搜索、日历等个人数据生态融合**。

**Claude：**

Anthropic 在 Agent API 层提供持久化 Memory Tool，让 Claude 可以在跨会话目录中创建、读取、更新和删除文件，不需要把所有内容一直放在 Context 中。



## **模块三：Task**

### 对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？

1. **目标常驻 System Prompt**

   每轮都注入任务目标、约束和完成标准。

   - 优点：简单直接，目标不容易完全丢失。
   - 缺点：占用 Context；目标过长时容易稀释当前信息，也不能记录执行进度。

2. **结构化 Plan/Todo**

   将任务拆成步骤，并记录 `pending/in_progress/completed` 状态，每轮只执行当前步骤。

   - 优点：进度清晰，容易恢复和审计。
   - 缺点：计划需要持续维护；过于固定可能限制动态调整。

3. **滚动摘要与 Checkpoint**

   定期把目标、已完成工作、关键结论、失败尝试和下一步压缩成检查点。

   - 优点：节省 Token，适合跨 Session 或进程恢复。
   - 缺点：摘要可能遗漏细节，多次压缩会产生信息漂移。

4. **外部持久化状态**

   将计划、代码、数据、决策记录等保存在数据库或文件中，需要时重新读取。

   - 优点：不依赖模型记忆，信息可追踪、可恢复。
   - 缺点：需要设计数据结构和读写规则；模型可能忘记及时更新状态。

5. **历史检索与 Memory 召回**

   根据当前步骤，从历史记录中召回相关事实、决策和工具结果。

   - 优点：适合很长的任务，不必加载全部历史。
   - 缺点：可能检索不到真正关键的信息，也可能召回过期或相似但错误的内容。

6. **Evaluator/Watchdog 校验**

   定期检查当前动作是否仍服务于原目标，是否满足约束和完成标准。

   - 优点：能够及时发现跑偏、循环和假完成。
   - 缺点：需要额外调用；Evaluator 和 Executor 可能犯相同错误。

7. **任务分解与子 Agent**

   把长任务拆成多个短而明确的子任务，每个 Agent 只处理有限范围，主 Agent 汇总结果。

   - 优点：单个 Context 更短，可并行执行。
   - 缺点：存在协调成本，子任务之间可能信息不一致或接口不匹配。



## **模块四：Tool / Session Runtime**

### Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？

采用“任务提交—后台执行—结果持久化—主动通知—上下文恢复”的设计。

1. **提交后立即返回**

   Agent 调用异步工具时只负责创建任务，立即返回 job_id、预计耗时和查询方式，告诉用户任务已在后台执行，避免阻塞当前对话。

2. **使用明确的任务状态机**

   任务状态包括：

   ```text
   queued → running → succeeded / failed / cancelled / timeout
   ```

   状态、进度、重试次数和错误信息都持久化到数据库，确保服务重启后仍可恢复。

3. **后台 Worker 执行**

   使用消息队列和 Worker 执行耗时任务。任务按 `job_id` 做幂等处理，支持超时、有限重试、取消和失败补偿，避免重复执行产生副作用。

4. **保存任务上下文**

   任务记录关联 `user_id`、`session_id`、`trace_id`、原始目标和工具参数。这样完成后能找到正确用户和会话，避免结果串到其他窗口。

5. **完成后主动通知**

   在线用户可通过 WebSocket 或 SSE 实时收到通知；离线用户可通过站内通知、邮件或其他授权渠道接收。通知应包含任务名称、完成状态、结果摘要和查看入口。

6. **结果重新进入 Agent 上下文**

   完整结果持久化保存，通知中只放摘要。用户回到会话后，Agent 将该异步任务结果作为 `tool_result` 或任务事件注入 Context，再继续原任务，而不是只显示一条孤立通知。

7. **避免打扰和过期结果**

   用户可以选择通知渠道和频率。完成时还要判断原任务是否已取消、结果是否过期；过期结果应标明时间，必要时重新执行。

8. **保证安全与可观测性**

   查询和通知必须校验任务归属，敏感结果不能直接出现在通知正文中。同时记录提交、开始、重试、完成和通知送达等 Trace，方便审计和排查。

核心原则是：**异步工具不占住对话，但任务状态和结果必须可靠持久化；完成后既通知用户，也恢复到原来的 Agent 任务上下文中。**



## **模块五：Agent Runtime 架构对比**

### Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？

1. **消息结构不同**

   Anthropic 把工具调用设计成消息中的内容块：

   ```
   assistant:
     - text
     - tool_use

   user:
     - tool_result
   ```

   `tool_use` 和 `tool_result` 通过 `tool_use_id` 关联，结果还可明确标记 `is_error`，并支持文本、图片等内容块。

   GLM、豆包的 OpenAI-compatible Chat API 通常采用：

   ```
   assistant.tool_calls
   tool:
     tool_call_id
     content
   ```

   函数参数一般放在 `function.arguments` 中，并以 JSON 字符串返回；执行结果通过独立的 `role="tool"` 消息回传。

2. **Claude 方案的优缺点**

   优点：

   - 工具调用、文本、图片和错误都是统一的 Content Block；
   - 能自然表示一轮中的多个工具调用和多模态工具结果；
   - 调用顺序和因果关系明确，适合复杂 Agent Loop；
   - `is_error` 是协议字段，不必自己约定错误 JSON。

   缺点：

   - 协议约束严格，`tool_result` 必须紧跟对应的 `tool_use`；
   - Context 压缩时不能拆散调用和结果；
   - 与 OpenAI SDK 生态不直接兼容，需要单独适配；
   - Runtime 和消息存储结构相对复杂。

3. **OpenAI-compatible 方案的优缺点**

   优点：

   - 数据结构扁平，工具循环实现简单；
   - OpenAI SDK、LangChain 等生态支持广泛；
   - 更换 GLM、豆包或其他兼容模型时，通常只需修改模型名和 `base_url`；
   - 很适合标准的“模型给参数—程序执行函数—结果返回模型”流程。

   缺点：

   - `arguments` 经常是 JSON 字符串，需要额外解析和校验；
   - 工具结果通常只是字符串，图片、错误和复杂结果需要自定义封装；
   - 各厂商只是“基本兼容”，在并行调用、流式输出、`tool_choice`、严格 Schema 等方面仍有差异；
   - 过度依赖兼容协议，可能无法利用某个模型的原生工具能力。

4. **为什么采用不同设计**

   Anthropic 更倾向把工具交互视为“多模态对话事件流”，强调内容块的组合能力和调用顺序；OpenAI-compatible 方案更像标准 RPC，强调接口统一、SDK 复用和接入成本。

5. **补充说明**

   豆包目前也提供 Responses API，使用 `function_call` 与 `function_call_output` 事件，通过 `call_id` 关联，已经不局限于传统的 `role="tool"` 格式。

总体而言：**Claude 原生协议表达能力更强，适合复杂 Agent；OpenAI-compatible 协议更简单通用，适合多模型快速接入。**
