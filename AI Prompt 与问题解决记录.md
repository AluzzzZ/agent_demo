# AI Prompt 与问题解决记录



## 原始agent框架搭建Prompt摘要

```text
请使用python从零实现一个最小可用Agent，不使用LangGraph、OpenHands、OpenClaw等现有Agent框架，核心 Agent Runtime和主循环必须自行实现。

要求：
1. 实现 Agent Loop：
   - 接收用户输入
   - 由真实LLM判断直接回复或调用工具
   - 执行工具
   - 将工具结果返回LLM
   - 继续循环或输出最终答案
   - 设置最大循环轮次，防止无限调用

2. 实现三个工具：
   - calculator
   - wiki search
   - weather

3. 实现统一工具注册机制。每个工具包含名称、描述、JSON参数Schema和执行函数，由LLM根据Schema自主选择工具并生成参数。

4. 实现LLM输出解析，识别：
   - 一个或多个工具调用
   - 最终答案
   不提取、不保存模型隐藏思维链。

5. 实现Session管理：
   - 隔离不同用户和窗口
   - 保存消息、摘要和工具状态
   - 支持程序重启后恢复对话
   - 支持普通追问和带工具结果的追问

6. 实现Context管理：
   - 每次调用LLM前召回当前Session的摘要、结构化状态和最近完整消息
   - Context超长时压缩较早历史
   - 不得拆分工具调用和对应结果
   - 不放入其他Session数据、API Key、完整Trace或隐藏思维链

7. 实现基础异常处理和结构化Trace：
   - 处理 LLM、工具、Schema、数据库和最大轮次异常
   - 工具错误以结构化结果返回LLM
   - Trace 记录模型调用、工具调用、耗时、轮次、结果和错误
   - 日志不得泄露密钥

8. 构建自动化测试，覆盖：
   - 直接回复
   - 单工具和多工具调用
   - 普通追问和工具追问
   - 多用户、多窗口Session隔离
   - 状态恢复
   - Context 压缩及工具消息配对
   - 异常处理
   - 最大轮次终止
   - Trace完整性

9. 默认测试使用Fake LLM，保证稳定且不产生费用；另提供需显式开启的真实LLM API smoke test。
```



## 约束Prompt摘要

用于指导本次实现的约束式Prompt：

```
开始修改前：
1. 检查当前代码结构、Git 状态、已有测试和未提交改动。
2. 保留用户已有改动，不覆盖或删除无关文件。
3. 找出Runtime、Provider、Tool Registry、Storage、Context、Trace、CLI、Web的边界。
4. 先说明准备修改的模块和设计，再实施代码。
5. 优先扩展现有实现，不进行无必要的大规模重构。
6. 不把 API Key、Token、密码或完整敏感工具参数写入日志、数据库、测试和Git。
7. 不记录模型隐藏思维链，只保存面向用户的答案、公开的决策摘要、工具调用和工具结果。

关键设计细节：
禁止使用Agent框架；SDK只能负责HTTP/API。
主循环必须与工具实现解耦；工具由name/description/input_schema/handler注册。
Session使用 (user_id, session_id) 隔离并可跨进程恢复。
不允许客户端通过修改session_id读取其他用户的数据
同一Session出现并发请求时应进行串行化或乐观锁控制，避免消息顺序错乱。
优先使用模型或SDK提供的token计数能力。
模型历史必须保留合法的tool_use/tool_result对，并由Provider Adapter转换协议。
只记录公开决策摘要，无需提取隐藏思维链。
所有关键行为必须能用Fake LLM稳定测试，真实API测试显式opt-in。

完成后必须：
1. 运行完整测试。
2. 报告通过、跳过和失败数量。
3. 执行Git diff格式检查。
4. 扫描代码和文档中的疑似API Key。
5. 列出实际修改的文件。
6. 说明关键设计决策。
7. 明确指出仍未覆盖的生产级能力。
8. 不自动运行可能产生费用的真实API测试。
9. 不自动提交或推送，除非得到明确授权。
```



## 异常处理Prompt摘要

```text
完善当前项目的异常处理机制：
1. 建立统一异常体系，包括：
   - LLM配置、认证、超时、限流和响应解析异常
   - 工具不存在、参数Schema校验失败和工具执行异常
   - Session、Context、数据库和最大循环轮次异常
2. Provider层将第三方SDK异常转换为项目内部异常。仅对超时、429和临时性5xx进行有限重试；认证及参数错误不重试。
3. 工具调用失败时不要直接中断 Agent，让 LLM 修正调用或向用户解释。
4. 达到最大轮次后受控终止，禁止无限循环。Context压缩失败时采用安全降级，保留最近完整消息以及配对的tool_use/tool_result。
5. 数据库异常必须回滚事务。CLI和Web API只能返回脱敏错误，不得暴露堆栈、API Key或内部凭据。
6. 每个请求生成trace_id。Trace记录异常类型、发生阶段、耗时及重试次数，但不得记录密钥。
7. 补充测试，覆盖超时重试、限流、认证失败、未知工具、参数错误、工具异常、最大轮次、压缩失败和日志脱敏。

测试要求：
- Provider 超时后有限重试
- 429 重试
- 认证失败不重试
- 非法模型响应
- 未知工具
- 工具参数 Schema 错误
- 工具 Handler 抛出异常
- 最大轮次终止
- Context 压缩失败降级
- Trace 中不包含 API Key
```



## Session管理Prompt摘要

```text
请完善当前项目的Session管理：
1. 使用(user_id, session_id)作为唯一隔离边界，确保不同用户、不同窗口之间的消息、摘要、Todo和工具状态互不影响。
2. 使用SQLite持久化Session和消息，支持：
   - 创建、查询、列表和更新Session
   - 继续历史对话
   - 更新标题
   - 归档或清空Session
   - 程序重启后恢复状态
3. 所有数据查询必须同时包含user_id和session_id。Web API从认证信息获取user_id，不能信任客户端传入的用户标识。
4. 同一Session的并发请求需要串行化或使用事务控制，保证消息和工具结果顺序正确；数据库操作失败时必须回滚，避免产生不完整数据。
5. Session只保存必要的对话和状态数据，不得保存API Key、隐藏思维链或完整Trace。

测试要求：
- 同一用户多窗口隔离
- 不同用户使用相同session_id时隔离
- Todo和工具状态隔离
- 重启后恢复对话
- 跨用户越权访问
- 并发消息顺序
- 数据库异常回滚

```



## Context管理Prompt摘要

```
请完善当前项目的Context管理：
1. 每次调用LLM前动态构建Context，包含：
   - System Prompt和工具Schema
   - 当前Session摘要
   - Todo等结构化状态
   - 最近的完整消息
   - 当前用户输入
   - 配对的tool_use/tool_result
2. Context必须按(user_id,session_id)隔离，不得包含其他会话数据、API Key、完整Trace或隐藏思维链。
3. 实现Token预算管理。Context超限时压缩较早历史，保留当前请求和最近完整对话；不得拆分工具调用及其结果，也不得丢失tool_call_id。
4. 摘要只保留用户目标、关键事实、工具结论、约束和未完成事项。新摘要应合并旧摘要，压缩失败时采用安全降级策略。
5. 每次LLM调用前召回当前Session的摘要、结构化状态和最近消息；仅在超过阈值时更新摘要，不要每轮无条件压缩。
6. 补充测试，覆盖长对话压缩、短对话不压缩、Session 隔离、普通追问、工具追问、摘要合并、多工具调用配对和压缩失败降级。

压缩策略：
1. 保留最近若干完整对话轮次。
2. 将更早历史压缩成Session summary。
3. 摘要只保留：
   - 用户目标
   - 已确认事实
   - 工具得到的重要结论
   - 用户偏好和约束
   - 未完成事项
   - 后续追问需要的信息
4. 摘要不得虚构信息。
5. 新摘要应合并旧摘要，避免每次从零总结全部历史。
6. 摘要成功后更新摘要覆盖到的消息位置或版本。
7. 原始消息可以继续保存在SQLite，但不必全部发送给LLM。
8. 压缩失败时使用确定性的本地降级摘要或安全裁剪。
9. 降级裁剪也必须保证工具调用配对完整。
10. Context压缩应生成脱敏Trace事件。


测试要求：
- 普通对话追问能使用历史
- 工具调用后的追问能使用工具结果
- 超长历史触发压缩
- 短历史不触发压缩
- 旧摘要与新内容正确合并
- 不召回其他 Session 内容
- tool_use/tool_result始终成对
- 并行工具调用结果不会错配
- 当前用户输入不会被裁剪
- 现有环境变量优先于.env
```



## Web工作台Prompt摘要

```text
搭建一个类似codex的前端工作台：
左上角Trace查看器。
左侧独立Session列表。
下侧聊天框，中央工具调用、快速示例和结果卡片。
左下角双用户登录、添加、退出和快速切换。
服务端通过Bearer Token解析用户，用户无法读取其他用户的Session、消息或Trace。
SQLite增加用户、密码摘要、登录Token和会话标题。
同一Session的并发请求在当前Web进程内串行执行。
工具异常采用稳定错误码，意外异常不会把内部错误或Secret返回模型。
Trace增加事件序号、失败状态及基础Secret脱敏。
```



## 主要问题与解决记录

### 1. Context 压缩破坏工具消息协议

**遇到的问题：**
Assistant 的 `tool_use` 和后续的 `tool_result` 必须成对出现。简单保留最后 N 条消息从工具结果中间切割，导致转换后的 API 请求格式无效。

**解决方案：**
 将裁剪位置回退到完整的用户对话轮次，确保 `tool_use`、`tool_result` 及其调用 ID 不被拆分。DashScope Adapter 再将其安全转换为 OpenAI 格式的 `assistant.tool_calls` 和 `tool` 消息，并通过测试验证压缩后仍能继续对话。

### 2. 多窗口 Session 状态难以可靠保存

**遇到的问题：**
全局变量只能服务单一进程，无法在程序重启后恢复；单个 JSON 文件在多个终端同时写入时容易发生覆盖或数据损坏。

**解决方案：**
使用 SQLite 持久化 Session、消息和 Todo，以 `(user_id, session_id)` 作为隔离边界。利用复合主键、事务和 WAL 支持多窗口访问与跨进程恢复，同时无需部署额外数据库服务。

### 3. 长期 Summary 应该放在 Context 的什么位置

**遇到的问题：**
如果把所有历史消息持续发送给模型，Context 会快速增长；如果把摘要混入普通消息，又可能破坏对话顺序和工具调用协议。

**解决方案：**
最近的完整消息继续使用原生 `messages`，保留正确的工具协议；长期摘要和 Todo 使用明确的 XML 标签放入 System Prompt，并在每次模型调用前召回。旧消息仍保存在 SQLite 中，只从发送给模型的 Context 中移除，从而兼顾审计、记忆连续性和调用成本。

### 4. 长期 Summary 应该放在 Context 的什么位置

**遇到的问题：**
如果把所有历史消息持续发送给模型，Context 会快速增长；把摘要混入普通消息，又破坏对话顺序和工具调用协议。

**解决方案：**
最近的完整消息继续使用原生 `messages`，保留正确的工具协议；长期摘要和 Todo 使用明确的 XML 标签放入 System Prompt，并在每次模型调用前召回。旧消息仍保存在 SQLite 中，只从发送给模型的 Context 中移除，从而兼顾审计、记忆连续性和调用成本。

### 5. LLM 连续调用工具可能形成无限循环

**遇到的问题：**
模型反复调用同一个工具，或者工具报错后不断使用相同参数重试，导致请求无法结束并持续消耗 Token。

**解决方案：**
在 `AgentRuntime` 中设置最大循环轮次。每完成一次 LLM 决策或工具执行就增加轮次计数，达到上限后受控终止，并向用户返回明确提示。Trace 同时记录终止原因、已执行轮次和最后一次工具调用。

### 6. 工具参数不一定符合 Schema

**遇到的问题：**
即使向 LLM 提供了工具 Schema，模型遗漏了必填字段、传入错误类型或生成工具不支持的参数。

**解决方案：**
所有工具在执行前统一经过JSON Schema校验。校验失败时不直接结束请求，而是生成带有 `is_error=true` 的结构化 `tool_result`，将安全的错误信息反馈给 LLM，使其有机会修正参数。

### 7. 多工具调用结果可能发生错配

**遇到的问题：**
模型一次可能调用多个工具。只按照工具名称或执行顺序保存结果，后续会把某个结果关联到错误的工具调用。

**解决方案：**
为每次工具调用保留唯一 `tool_call_id`，工具结果通过该 ID 与调用一一对应。即使多个工具并行或顺序执行，Provider 转换、数据库持久化和 Context 构建都必须保留这个关联关系。

### 8. 同一 Session 的并发请求可能打乱消息顺序

**遇到的问题：**
用户在同一个窗口快速发送多个请求，如果这些请求同时执行，后一个请求可能先完成，导致历史消息、工具结果和最终答案顺序错乱。

**解决方案：**
针对 `(user_id, session_id)` 建立独立锁，使同一 Session 的请求串行执行，不同 Session 仍可并行处理。数据库层同时使用事务和明确的消息时间或序号维持稳定顺序。

### 9. 不同 LLM Provider 的工具协议不一致

**遇到的问题：**
Agent Runtime 使用统一的 `tool_use/tool_result` 结构，但不同模型供应商对工具调用的字段、消息角色和结束原因定义不同。

**解决方案：**
在 Runtime 与供应商 SDK 之间增加 Provider Adapter。Runtime 只处理统一的内部消息格式，由 Adapter 负责转换工具 Schema、`tool_calls`、工具结果和结束原因，使核心循环不依赖具体模型供应商。

### 10. 工具返回内容可能过长

**遇到的问题：**
Search工具可能返回大量文本。如果将完整结果持续放入 Context，会迅速占满 Token 预算，影响模型理解和后续追问。

**解决方案：**
工具层保留完整原始结果用于审计，发送给 LLM 时只保留必要字段、限制结果数量并截断超长文本。重要结论可在后续压缩时进入 Session Summary，避免每轮重复发送完整内容。

### 11. 摘要更新可能丢失关键状态

**遇到的问题：**
简单总结历史可能遗漏用户约束、未完成事项或重要工具结论，导致后续追问出现“失忆”。

**解决方案：**
使用固定摘要结构，只提取用户目标、关键事实、约束、工具结论和未完成事项。新摘要必须合并旧摘要，而不是直接覆盖。Todo 等确定性状态独立保存，不依赖自然语言摘要恢复。

### 12. Trace 容易记录敏感信息

**遇到的问题：**
工具参数、异常对象或模型请求中可能包含 API Key、密码和其他敏感信息。如果直接序列化到 Trace，会造成凭据泄露。

**解决方案：**
所有 Trace 事件写入前经过统一脱敏，只记录工具名称、调用 ID、耗时、执行状态和安全摘要。对 `api_key`、`token`、`password`、`authorization` 等字段进行屏蔽，不记录完整请求头、隐藏思维链或异常堆栈.

### 13. 使用真实 LLM 运行测试成本高且结果不稳定

**遇到的问题：**
单元测试依赖真实模型会产生费用，运行速度较慢，而且模型输出具有随机性，难以稳定覆盖所有 Runtime 分支。

**解决方案：**
默认使用脚本化 Fake LLM 精确模拟直接回复、工具调用、参数错误和最大轮次等场景。另提供需要显式开启的真实 DashScope smoke test，用于验证真实模型和工具调用协议是否可用。
